import React from 'react';

const ImageScore = (props) => {
// props.ups is number of up votes
// props.downs is number of downvotes

    return(
      <div>
        <div className="progress">
        <div className =
      </div>

    )

};


export default ImageScore;
